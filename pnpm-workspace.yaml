packages:
  - "apps/*"
  - "packages/*"
  - "scripts/*"
# Both fetch a native binary in their postinstall: esbuild for vitest/vite,
# workerd for the Cloudflare Worker runtime that `wrangler dev` runs on.
allowBuilds:
  esbuild: true
  workerd: true
